# ~/.tmuxinator/ygo-web.yml
# you can make as many tabs as you wish...

project_name: ygo-web
project_root: /Users/akira/Dropbox/work/02.products/01.YourGolf/YGO-web
pre: source script/export-envs.sh
tabs:
  - main:
      layout: main-horizontal
      panes:
        - source script/export-envs.sh && rails s -d
        - source script/export-envs.sh && bundle exec guard
        - #empty, will just run plain bash
        - #empty, will just run plain bash
  - shell: cd /Users/akira/.rvm/gems/ruby-1.9.3-p125/gems && vim
